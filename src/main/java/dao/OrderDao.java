package dao;

import bean.Order;

public class OrderDao {
    public Integer bulidOrder(Order order,DatabaseDao databaseDao){
        String sql="insert into user(type,name,password,salt,email,phone,enable,headIconUrl) values('\"+"
    }
}
